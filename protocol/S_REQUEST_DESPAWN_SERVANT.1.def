# doesn't seem to be triggered when servant gets out of view (like npc/users do), but only when manually dismissing it

uint64 gameId # matches gameId in S_REQUEST_SPAWN_SERVANT
int32 unk2 # 0
int16 unk3 # 0
